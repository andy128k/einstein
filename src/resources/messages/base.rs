pub struct Messages<'a> {
    pub rules: &'a str,
    pub close: &'a str,
    pub next: &'a str,
    pub prev: &'a str,
    pub ok: &'a str,
    pub cancel: &'a str,
    pub fullscreen: &'a str,
    pub options: &'a str,
    pub new_game: &'a str,
    pub load_game: &'a str,
    pub top_scores: &'a str,
    pub about: &'a str,
    pub exit: &'a str,
    pub einstein_puzzle: &'a str,
    pub version: &'a str,
    pub copyright: &'a str,
    pub anonymous: &'a str,
    pub enter_name: &'a str,
    pub empty: &'a str,
    pub enter_game: &'a str,
    pub save_error: &'a str,
    pub save_game: &'a str,
    pub paused: &'a str,
    pub won: &'a str,
    pub start_new: &'a str,
    pub try_again: &'a str,
    pub loose: &'a str,
    pub pause: &'a str,
    pub switch: &'a str,
    pub save: &'a str,
    pub help: &'a str,
    pub loading: &'a str,
    pub einstein_flowix: &'a str,
    pub enable_sound: &'a str,
    pub volume: &'a str,
}
